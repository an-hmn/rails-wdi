%table#users
  - @users.each do |user|
    %tr
      %td= user.name
      %td= image_tag(user.image)
      %td= link_to('Delete', user, :method => :delete, :confirm => 'Really?', :class => 'button radius alert tiny')
